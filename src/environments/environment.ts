// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyA0DSJr8UjCGO4BAW40LbeN-Mv8uO6h5BE",
    authDomain: "personal-website-7165a.firebaseapp.com",
    databaseURL: "https://personal-website-7165a.firebaseio.com",
    projectId: "personal-website-7165a",
    storageBucket: "personal-website-7165a.appspot.com",
    messagingSenderId: "259019036947"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
